# Leave unset on Vercel to use built-in demo data (History, Compare, Reports, Q&A stubs).
# For local dev with FastAPI backend, copy .env.local.example to .env.local:
#   NEXT_PUBLIC_API_URL=http://localhost:8000
#   NEXT_PUBLIC_WS_URL=ws://localhost:8000
#
# For production with live uploads, set to your deployed backend (Render/Railway):
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
# NEXT_PUBLIC_WS_URL=wss://your-backend.onrender.com
