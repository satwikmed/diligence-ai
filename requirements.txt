# Diligence AI — core runtime (installs in ~1-2 minutes)
# Works in DEMO_MODE without API keys. Uses pypdf fallback for PDF parsing.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=23.2.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
openai>=1.30.0
pypdf>=4.0.0
reportlab>=4.1.0

# Optional but lightweight — used when OPENAI_API_KEY is set
langchain-openai>=0.1.0

# Optional services — only needed with real API keys
pinecone-client>=3.2.0
apify-client>=1.6.0

# Dev / seed / tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
